var nome = 'Leticia';
var idade = '22';
var possuiFaculdade = true;

console.log(nome, idade, possuiFaculdade, nome);

var preco = 25
var totalComprado = 5;
var totalPreco = totalComprado * preco

console.log(totalPreco);

// podemos criar várias variáveis de uma vez separando por vírgula apenas

var sobrenome = 'Lopes', 
    cidade = 'Rio';

console.log(sobrenome, cidade);

// podemos criar uma variável sem definir algum valor inicialmente (variáveis globais no início do js, por exemplo)

var semDefinir;

console.log(semDefinir);


// EXERCISES:

// Declarar uma variável com o seu nome
var name = 'leticia';

// Declarar uma variável com a sua idade
var age = 22;

// Declarar uma variável com a sua comida favorita e não atribuir valor
var favoriteMeal;

// Atribuir valor a sua comida favorita
favoriteMeal = 'barbecue';

// Declarar 5 variáveis diferentes sem valores
var kiwi = true, 
    strawberry, 
    grape, 
    orange, 
    lemon;

console.log(name, age, favoriteMeal, kiwi)